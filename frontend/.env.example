# ===========================================
# 클래스 플래너 프로젝트 환경 변수 설정
# ===========================================

# ===========================================
# Supabase 설정 (프론트엔드 - GitHub Pages)
# ===========================================
# Supabase 프로젝트 URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase 공개 키 (anon key) - 브라우저에서 사용
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ===========================================
# Supabase 설정 (백엔드 - Vercel)
# ===========================================
# Supabase 프로젝트 URL (서버리스 함수에서 사용)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase 서비스 키 (서버에서 사용) - 관리자 권한
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ===========================================
# Vercel 설정 (선택사항)
# ===========================================
# Vercel 프로젝트 ID
# VERCEL_PROJECT_ID=your-vercel-project-id

# Vercel 팀 ID (팀 계정 사용 시)
# VERCEL_TEAM_ID=your-vercel-team-id

# ===========================================
# 개발 환경 설정
# ===========================================
# 개발 모드 여부
# NODE_ENV=development

# 포트 설정 (기본값: 5173)
PORT=5173

# 자동화 테스트용 계정 정보
TEST_EMAIL=your-test-email@gmail.com
TEST_PASSWORD=your-test-password


# ===========================================
# 사용법:
# ===========================================
# 1. 이 파일을 .env.local로 복사
# 2. Supabase 대시보드에서 실제 값으로 교체
# 3. Vercel 대시보드에서 환경 변수 설정
# 4. GitHub Secrets에 VITE_SUPABASE_* 변수 설정
#
# 주의사항:
# - .env.local은 .gitignore에 의해 Git에서 제외됨
# - VITE_ 접두사는 프론트엔드에서만 사용
# - SUPABASE_SERVICE_ROLE_KEY는 절대 공개하지 마세요
# ===========================================
