/**
 * Pretendard font Base64 subset for jsPDF Korean rendering.
 * Currently a placeholder — jsPDF will fall back to Helvetica (Latin only).
 * To enable Korean: run `pyftsubset` on PretendardVariable.ttf and paste base64 here.
 */
export const PRETENDARD_BASE64 = "";
